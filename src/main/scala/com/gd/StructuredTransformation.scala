package com.gd

import org.apache.spark.sql.{DataFrame, SparkSession}

class StructuredTransformation {

  def transform(df: DataFrame): DataFrame = {
    df
  }

//  def process(): Unit = {
//    val s: DataFrame = spark.readStream.json("")
//  }
}
